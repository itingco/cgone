@extends('layouts.app') @section('title','Users') @section('content')<div class="text-end mb-3"><a class="btn btn-primary" href="{{ route('config.users.create') }}">New User</a></div><div class="card"><table class="table mb-0"><thead><tr><th>Name</th><th>Email</th><th>Roles</th><th>Status</th><th>Action</th></tr></thead><tbody>@foreach($rows as $r)<tr><td>{{ $r->name }}</td><td>{{ $r->email }}</td><td>{{ $r->roles->pluck('name')->join(', ') }}</td><td><span class="badge {{ $r->is_active?'bg-success':'bg-secondary' }}">{{ $r->is_active?'Active':'Inactive' }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('config.users.edit',$r) }}">Edit</a></td></tr>@endforeach</tbody></table></div><div class="mt-3">{{ $rows->links() }}</div>@endsection
