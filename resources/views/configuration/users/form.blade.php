@extends('layouts.app') @section('title',$isEdit?'Edit User':'New User') @section('content')<div class="card"><div class="card-body"><form method="post" action="{{ $isEdit?route('config.users.update',$record):route('config.users.store') }}">@csrf @if($isEdit)@method('PUT')@endif<div class="row g-3"><div class="col-md-6"><label class="form-label">Name</label><input class="form-control" name="name" value="{{ old('name',$record->name) }}" required></div><div class="col-md-6"><label class="form-label">Email</label><input type="email" class="form-control" name="email" value="{{ old('email',$record->email) }}" required></div><div class="col-md-6"><label class="form-label">Password {{ $isEdit?'(leave blank to keep)':'' }}</label><input type="password" class="form-control" name="password" @if(!$isEdit) required @endif></div><div class="col-md-6"><label class="form-label">Roles</label><select class="form-select" name="role_ids[]" multiple size="5">@foreach($roles as $role)<option value="{{ $role->id }}" @selected(in_array($role->id,old('role_ids',$record->roles->pluck('id')->all())))>{{ $role->code }} - {{ $role->name }}</option>@endforeach</select></div><div class="col-12"><input type="hidden" name="is_active" value="0"><label><input type="checkbox" name="is_active" value="1" @checked((bool)old('is_active',$record->exists?$record->is_active:true))> Active</label></div></div><div class="mt-4"><button class="btn btn-primary">Save</button><a class="btn btn-outline-secondary" href="{{ route('config.users.index') }}">Cancel</a></div></form></div></div>@endsection
