@extends('layouts.app')
@section('title','Price Updates')
@section('content')
<div class="d-flex justify-content-between mb-3 gap-2 flex-wrap"><div><h4 class="mb-0">Price Updates</h4><div class="small text-muted">Upload Excel, validate, then Release. Released items remain price-locked until each item is Approved or Rejected.</div></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('price-approval.index') }}">Price Approval</a><a class="btn btn-outline-success" href="{{ route('price-updates.template') }}">Download Excel Template</a></div></div>
<div class="card mb-3"><div class="card-body"><form method="post" enctype="multipart/form-data" action="{{ route('price-updates.import') }}">@csrf<div class="row g-2 align-items-end"><div class="col-md-4"><label class="form-label">Description</label><input class="form-control" name="description" placeholder="September 2026 Price Update" required></div><div class="col-md-5"><label class="form-label">Excel .xlsx</label><input type="file" class="form-control" name="file" accept=".xlsx" required></div><div class="col-md-3"><button class="btn btn-primary">Upload & Validate</button></div></div></form></div></div>
<div class="card"><div class="table-responsive"><table class="table mb-0 align-middle"><thead><tr><th>Batch</th><th>Description</th><th>Total</th><th>Pending</th><th>Approved</th><th>Rejected</th><th>Status</th><th>Uploaded</th></tr></thead><tbody>@forelse($rows as $b)<tr><td><a href="{{ route('price-updates.show',$b) }}">{{ $b->batch_no }}</a></td><td>{{ $b->description }}</td><td>{{ $b->lines_count }}</td><td><span class="badge text-bg-warning">{{ $b->pending_lines_count }}</span></td><td><span class="badge text-bg-success">{{ $b->approved_lines_count }}</span></td><td><span class="badge text-bg-danger">{{ $b->rejected_lines_count }}</span></td><td><span class="status-badge status-{{ $b->status }}">{{ $b->status }}</span></td><td>{{ $b->created_at }}</td></tr>@empty<tr><td colspan="8" class="text-center text-muted">No price updates</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $rows->links() }}</div>
@endsection
