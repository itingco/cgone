@extends('layouts.app') @section('title','New Reversal') @section('content')<div class="row"><div class="col-lg-7"><div class="card"><div class="card-body"><p class="text-muted">Enter the posted ledger type and source ID. The system creates an opposite entry and preserves the original.</p><form method="post" action="{{ route('adjustments.store') }}">@csrf<div class="mb-3"><label class="form-label">Ledger Type</label><select name="ledger_type" class="form-select" required>@foreach(['item'=>'Item Ledger','customer'=>'Customer Ledger','vendor'=>'Vendor Ledger','gl'=>'General Ledger Batch'] as $v=>$label)<option value="{{ $v }}" @selected(old('ledger_type',$type)===$v)>{{ $label }}</option>@endforeach</select></div><div class="mb-3"><label class="form-label">Source Entry / Batch ID</label><input class="form-control" type="number" min="1" name="source_entry_id" value="{{ old('source_entry_id',$sourceId) }}" required></div><div class="mb-3"><label class="form-label">Reason</label><textarea class="form-control" name="reason" rows="4" minlength="10" required>{{ old('reason') }}</textarea><div class="form-text">Minimum 10 characters.</div></div><button class="btn btn-danger">Post Reversal</button><a class="btn btn-outline-secondary" href="{{ route('adjustments.index') }}">Cancel</a></form></div></div></div></div>@endsection
