@extends('layouts.app')
@section('title','Employee Allocations')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h4 class="mb-1">Employee Allocation History</h4><div class="text-muted small">Effective-dated organization history used by backdated payroll/reporting.</div></div><a class="btn btn-primary" href="{{ route('hr.allocations.create') }}">+ New Allocation</a></div>
<form class="mb-3" style="max-width:520px"><select class="form-select" name="employee_id" onchange="this.form.submit()"><option value="">All Employees</option>@foreach($employees as $e)<option value="{{ $e->id }}" @selected((string)request('employee_id')===(string)$e->id)>{{ $e->employee_code }} - {{ $e->full_name }}</option>@endforeach</select></form>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Employee</th><th>Effective</th><th>BU</th><th>Department</th><th>Position</th><th>Office</th><th>Payroll Group</th><th></th></tr></thead><tbody>@forelse($rows as $a)<tr><td><a href="{{ route('hr.employees.show',$a->employee) }}">{{ $a->employee->employee_code }} - {{ $a->employee->full_name }}</a></td><td>{{ optional($a->effective_from)->format('d/m/Y') }} → {{ optional($a->effective_to)->format('d/m/Y') ?: 'Current' }}</td><td>{{ $a->businessUnit?->code ?? '-' }}</td><td>{{ $a->department?->name ?? '-' }}</td><td>{{ $a->position?->name ?? '-' }}</td><td>{{ $a->officeLocation?->name ?? '-' }}</td><td>{{ $a->payrollGroup?->name ?? '-' }}</td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('hr.allocations.edit',$a) }}">Edit</a></td></tr>@empty<tr><td colspan="8" class="text-center text-muted py-4">No allocation data.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $rows->links() }}</div>
@endsection
