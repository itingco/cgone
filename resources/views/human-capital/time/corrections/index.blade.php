@extends('layouts.app')
@section('title','Attendance Corrections')
@section('content')
<div class="mb-3"><h4 class="mb-1">Attendance Corrections</h4><div class="small text-muted">Workflow: DRAFT → SUBMITTED → APPROVED / REJECTED. Approval changes effective payroll input, not raw attendance.</div></div>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Employee</th><th>Work Date</th><th>Raw</th><th>Requested</th><th>Reason</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($rows as $c)<tr><td>{{ $c->attendance?->employee?->employee_code }} - {{ $c->attendance?->employee?->full_name }}</td><td>{{ optional($c->attendance?->work_date)->format('d/m/Y') }}</td><td>{{ optional($c->attendance?->raw_check_in)->format('H:i') ?: '-' }} → {{ optional($c->attendance?->raw_check_out)->format('H:i') ?: '-' }}</td><td>{{ optional($c->requested_check_in)->format('H:i') ?: '-' }} → {{ optional($c->requested_check_out)->format('H:i') ?: '-' }}</td><td>{{ $c->reason }}</td><td><span class="badge text-bg-secondary">{{ $c->status }}</span></td><td><div class="d-flex flex-wrap gap-1">@if($c->status==='DRAFT')<form method="post" action="{{ route('hr.attendance-corrections.submit',$c) }}">@csrf<button class="btn btn-sm btn-outline-primary">Submit</button></form>@endif @if($c->status==='SUBMITTED')<form method="post" action="{{ route('hr.attendance-corrections.approve',$c) }}">@csrf<button class="btn btn-sm btn-success">Approve</button></form><form method="post" action="{{ route('hr.attendance-corrections.reject',$c) }}">@csrf<input type="hidden" name="reason" value="Rejected during review"><button class="btn btn-sm btn-outline-danger">Reject</button></form>@endif</div></td></tr>@empty<tr><td colspan="7" class="text-center text-muted py-4">No correction requests.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $rows->links() }}</div>
@endsection
