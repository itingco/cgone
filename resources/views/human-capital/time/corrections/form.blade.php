@extends('layouts.app')
@section('title','Attendance Correction')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h4 class="mb-0">Attendance Correction</h4><div class="small text-muted">{{ $attendance->employee?->employee_code }} - {{ $attendance->employee?->full_name }} · {{ optional($attendance->work_date)->format('d/m/Y') }}</div></div><a class="btn btn-outline-secondary" href="{{ route('hr.attendance.index') }}">Back</a></div>
<div class="card"><div class="card-body"><div class="alert alert-light border">Raw attendance: <strong>{{ optional($attendance->raw_check_in)->format('d/m/Y H:i') ?: '-' }}</strong> → <strong>{{ optional($attendance->raw_check_out)->format('d/m/Y H:i') ?: '-' }}</strong></div><form method="post" enctype="multipart/form-data" action="{{ route('hr.attendance-corrections.store') }}">@csrf<input type="hidden" name="attendance_record_id" value="{{ $attendance->id }}"><div class="row g-3"><div class="col-md-4"><label class="form-label">Requested Check In</label><input type="datetime-local" class="form-control" name="requested_check_in" value="{{ optional($attendance->raw_check_in)->format('Y-m-d\TH:i') }}"></div><div class="col-md-4"><label class="form-label">Requested Check Out</label><input type="datetime-local" class="form-control" name="requested_check_out" value="{{ optional($attendance->raw_check_out)->format('Y-m-d\TH:i') }}"></div><div class="col-md-4"><label class="form-label">Attachment</label><input type="file" class="form-control" name="attachment"></div><div class="col-12"><label class="form-label">Reason</label><textarea class="form-control" name="reason" required></textarea></div></div><button class="btn btn-primary mt-3">Create Draft</button></form></div></div>
@endsection
