@extends('layouts.app')
@section('title','Edit Schedule Assignment')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h4 class="mb-0">Edit Schedule Assignment</h4><div class="small text-muted">Close an old effective period before starting a new overlapping assignment.</div></div><a class="btn btn-outline-secondary" href="{{ route('hr.schedules.index') }}">Back</a></div>
<div class="card"><div class="card-body"><form method="post" action="{{ route('hr.schedules.assignments.update',$assignment) }}">@csrf @method('PUT')<div class="row g-3"><div class="col-md-5"><label class="form-label">Employee</label><select class="form-select" name="employee_id" required>@foreach($employees as $e)<option value="{{ $e->id }}" @selected((int)$assignment->employee_id===(int)$e->id)>{{ $e->employee_code }} - {{ $e->full_name }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label">Pattern</label><select class="form-select" name="shift_pattern_id" required>@foreach($patterns as $p)<option value="{{ $p->id }}" @selected((int)$assignment->shift_pattern_id===(int)$p->id)>{{ $p->code }} - {{ $p->name }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Effective From</label><input type="date" class="form-control" name="effective_from" value="{{ optional($assignment->effective_from)->format('Y-m-d') }}" required></div><div class="col-md-2"><label class="form-label">Effective To</label><input type="date" class="form-control" name="effective_to" value="{{ optional($assignment->effective_to)->format('Y-m-d') }}"></div><div class="col-12"><label class="form-label">Notes</label><textarea class="form-control" name="notes">{{ $assignment->notes }}</textarea></div></div><button class="btn btn-primary mt-3">Save Assignment</button></form></div></div>
@endsection
