@extends('layouts.app') @section('title','Posting Preview') @section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><div class="text-muted small">Posting Preview</div><h3>{{ $doc->document_no }}</h3></div><span class="status-badge status-RELEASED">RELEASED</span></div>
<div class="card mb-3"><div class="card-body d-flex justify-content-between align-items-center"><div><div class="text-muted small">Expected Posted Document No.</div><div class="fs-5 fw-bold">{{ $expectedPostedNo }}</div></div><div class="small text-muted text-end">Final number is assigned atomically when POST is confirmed.</div></div></div><div class="alert alert-warning border-0"><strong>Review before POST.</strong> Posting is atomic: posted snapshot, ledger entries, GL and audit must all succeed or everything is rolled back.</div>
<div class="row g-3"><div class="col-lg-7"><div class="card"><div class="card-header bg-white"><strong>General Ledger Preview</strong></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Account ID</th><th>Description</th><th class="text-end">Debit</th><th class="text-end">Credit</th></tr></thead><tbody>@foreach($preview['gl'] as $line)<tr><td>{{ $accounts[$line['account_id']]->code ?? $line['account_id'] }} - {{ $accounts[$line['account_id']]->name ?? '' }}</td><td>{{ $line['description'] }}</td><td class="text-end">{{ number_format((float)$line['debit'],2,',','.') }}</td><td class="text-end">{{ number_format((float)$line['credit'],2,',','.') }}</td></tr>@endforeach</tbody><tfoot><tr class="fw-bold"><td colspan="2">Total</td><td class="text-end">{{ number_format(collect($preview['gl'])->sum('debit'),2,',','.') }}</td><td class="text-end">{{ number_format(collect($preview['gl'])->sum('credit'),2,',','.') }}</td></tr></tfoot></table></div></div></div><div class="col-lg-5"><div class="card"><div class="card-body"><h5>Posting Result</h5><ul class="text-muted"><li>Posted document snapshot</li>@if(isset($preview['item_movements']))<li>Item Ledger movement</li>@endif @if(isset($preview['customer_ledger']))<li>Customer Ledger entry</li>@endif @if(isset($preview['vendor_ledger']))<li>Vendor Ledger entry</li>@endif<li>Balanced General Ledger batch</li><li>User activity log</li></ul><form method="post" action="{{ route('posting.post',[$type,$doc->id]) }}">@csrf<button class="btn btn-warning w-100" onclick="return confirm('POST this document? It can only be corrected with UNDO.')">POST DOCUMENT</button></form></div></div></div></div>
@endsection
