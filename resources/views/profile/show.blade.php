@extends('layouts.app')
@section('title','My Profile')
@section('content')
<div class="row g-4"><div class="col-xl-8"><div class="card"><div class="card-body"><div class="d-flex align-items-center gap-3 mb-4"><div class="rounded-circle bg-dark text-white d-grid align-items-center justify-content-center" style="width:58px;height:58px;font-size:1.25rem">{{ strtoupper(substr($user->name,0,1)) }}</div><div><h4 class="mb-1">{{ $user->name }}</h4><div class="text-muted">{{ $user->email }}</div></div></div><div class="row g-3"><div class="col-md-6"><div class="text-muted small">Department</div><div class="fw-semibold">{{ $user->department?->name ?? '-' }}</div></div><div class="col-md-6"><div class="text-muted small">Account Status</div><div><span class="badge {{ $user->is_active?'text-bg-success':'text-bg-secondary' }}">{{ $user->is_active?'Active':'Inactive' }}</span></div></div><div class="col-12"><div class="text-muted small mb-2">Roles</div><div class="d-flex flex-wrap gap-2">@forelse($user->roles as $role)<span class="badge text-bg-light border">{{ $role->name }}</span>@empty<span class="text-muted">No role</span>@endforelse</div></div></div></div></div></div><div class="col-xl-4"><div class="card"><div class="card-body"><h5>Account Security</h5><p class="text-muted small">Change your password periodically and never share your account.</p><a class="btn btn-outline-primary w-100" href="{{ route('profile.password') }}">Change Password</a></div></div></div></div>
@endsection
